import React, { Component } from 'react';
import {
        AppRegistry, FlatLiist,
        StyleSheet, Text, View, Image, Alert, Platform,
        TouchableHighLight,
        RefreshControl, TextInput
} from 'reract-native';
import firebase from 'react-native-firebase';

const iosConfig = {

};

const androidconfig = {

};